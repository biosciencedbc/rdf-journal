<%
def c(str)
  if str.nil?
    ""
  else
    str.lstrip
  end
end
%>
<http://www.ncbi.nlm.nih.gov/nlmcatalog/<%=c nlmid %>> a bibo:Journal;
  dcterms:title "<%=c jt %>" ;
  fabio:hasNLMJournalTitleAbbreviation "<%=c medabbr %>" ;
  <% if !issn_p.empty? %>bibo:isbn "<%=c issn_p %>" ; <% end %>
  <% if !issn_o.empty? %>prism:eISSN "<%=c issn_o %>" ; <% end %>
  fabio:hasNationalLibraryOfMedicineJournalId "<%=c jid %>" .


